# Nicky Portfolio

This is a lightweight static portfolio scaffold for Nicky.

Open `index.html` in a browser to preview. Customize the `index.html` and images in `assets/images/`.

To initialize locally (already done):

```powershell
cd 'C:\Users\ICT\Desktop\nicky-portfolio'
# repo already initialized and committed by the assistant
```

Next steps:
- Replace placeholder project images in `assets/images/`.
- Customize copy in `index.html`.
- Optionally, push to GitHub and enable Pages.
